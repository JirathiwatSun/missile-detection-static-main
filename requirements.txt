# Missile Detection Project Requirements
# Default CPU Install:    pip install -r requirements.txt
#
# FOR NVIDIA GPU USERS:
# If you have an NVIDIA GPU, you must uninstall the default PyTorch and install the CUDA version:
#   1. pip uninstall -y torch torchvision torchaudio
#   2. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.3.0
roboflow>=1.2.0
opencv-python
torch>=2.0.0
torchvision
numpy
pandas
matplotlib
psutil
py-cpuinfo
pyyaml
requests
scipy
seaborn
tqdm
